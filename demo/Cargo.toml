[package]
name   ="demo"
version="0.1.0"
edition="2021"

[dependencies]
edge-frontend={ path="../frontend", features=["demo"] }
clap = {version = "4.5", features = ["derive"]}
bincode = {workspace = true}
